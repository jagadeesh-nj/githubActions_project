name: environment_Variable
on: workflow_dispatch
env:
  env_var: ${{vars.YOUR_OS}}

jobs: 
 testingVariables:
  runs-on: ubuntu-latest
  environment: Testing_Environment 
  env:
    env_var: ${{vars.YOUR_OS}}
  steps:
    - name: setting up environment_Variable
      run: echo "so u studied ${{ vars.YOUR_EDUCATION }} on ${{ vars.FIRST_DAY }}"
